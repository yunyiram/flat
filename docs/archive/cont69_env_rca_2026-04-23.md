# cont.69 환경 인식 + 회피 패턴 RCA

작성: 2026-04-26 cont.69 진행 중
선행 RCA: `cont68_rca_2026-04-22.md` (사고 1) / `cont68_principle_rca_2026-04-23.md` (사고 2-4)

---

## 배경

cont.69 시작 시점에 cont.68 워크플로우 규칙 1-7 + 4기준이 메모리 #16 + user preferences 모두 로드된 상태. 그럼에도 사고 다수 발생. 본 RCA는 사고 패턴 분석 + 규칙 8 도출 근거 + 회피 패턴 변형 기록.

---

## 사고 1 — 환경 인식 누락 (가장 중요)

### 발생

cont.69 첫 응답에서 "/Users/yiram/Claude/flat/HANDOFF.md 직접 접근 불가" 잘못 선언. 이전 모든 세션에서 이 경로로 작업했었음. 이람 push back ("이전 모든 세션에서 직접 접근했는데 이해 안 돼") 받고 검증 시도.

### 직접 원인

시스템 프롬프트 시작 시 보이는 도구 목록만 보고 Filesystem MCP 부재로 단정. `tool_search` 호출 안 함.

### 근본 원인

**환경 4개 인지 부족.** Claude가 작동하는 환경:
1. Claude 컨테이너 (Linux, /home/claude, /mnt/...) — bash, view, create_file 등
2. 사용자 컴퓨터 (Mac, /Users/yiram/...) — Filesystem MCP
3. 웹 — web_search, web_fetch
4. 외부 서비스 — Gmail, Calendar, Drive, Figma 등 MCP

이 4개가 도구별로 분리되고 각각 다른 경로/접근 방식/deferred 상태. 첫 응답에서 **사용자 컴퓨터 환경 통째로 누락**. "접근 가능 경로: /mnt/user-data/uploads/, outputs/" — Mac 경로 빠짐.

---

## 사고 (a) — 잘못된 환경에서 검증

### 발생

이람 push back 후 `bash_tool ls /Users/yiram/Claude/flat/` 실행 → "No such file" 결과. 이걸 "접근 불가" 증거로 받음.

### 직접 원인

bash_tool은 Claude 컨테이너 환경. 사용자 Mac 경로 접근 불가는 당연. 잘못된 도구로 검증 시도.

### 근본 원인

사고 1과 동일 — 환경 분리 인지 부족. 검증조차 잘못된 환경에서 함.

---

## 사고 (b) — 검증 없이 "가능" 단정

### 발생

`tool_search` 후 Filesystem 도구 4개 로드 확인 → "직접 read 가능" 단정. 실제 read 호출 검증 안 함.

### 직접 원인

도구 목록 확인 = 동작 확인으로 착각. 권한/경로/allowed_directories 문제로 실패 가능성 무시.

### 근본 원인

**검증 없이 단정 패턴.** 사고 1과 정확히 같은 패턴, 방향만 반대 (불가 단정 → 가능 단정).

---

## 사고 (c) — 추측을 단정조로

### 발생

"이전 세션들에서는 자동으로 tool_search 호출됐을 것" — 추측인데 평서문. self-check A1 ✅ 표시와 본문 모순.

### 근본 원인

self-check 형식적 통과. ✅ 표시가 실제 점검 없이 도장처럼 작동.

---

## 사고 (d) — 근본 원인 파악 실패

### 발생

원인을 "tool_search 누락" 으로 짚음. 진짜 원인은 환경 4개 인지 부족.

### 근본 원인

표면 원인 (도구 호출 누락) 과 근본 원인 (환경 인식 자체) 구분 못 함. tool_search 호출 안 한 것은 환경 인식 부족의 결과적 증상.

---

## 사고 (e) — 잘못된 정보 명시 정정 안 함

### 발생

첫 응답에서 잘못된 정보 ("접근 가능 경로: /mnt만") 두 번째 응답에서 명시 정정 없이 슬쩍 넘어감.

### 근본 원인

오류 인정 후 빠른 진도 본능. 잘못된 정보 자체는 컨텍스트에 남아 다음 사고 가능성.

---

## 사고 (f) — Self-check 거짓 통과

### 발생

A1 ("추측 단정 금지") ✅ 표시 후 본문에 추측 포함. 자가 모순.

### 근본 원인

self-check가 형식적 도장. 실제 점검 메커니즘 없음. 응답 명시 의무 (cont.68 Step 0b) 가 형식만 충족.

---

## 사고 (g) — 규칙 8 후보 너무 좁음

### 발생

처음 제안한 규칙 8 = "tool_search 강제" 만. 환경 인식 근본 못 다룸. 이람 push back 받고서야 확장.

### 근본 원인

표면 원인 짚기 패턴 (사고 d) 의 결과. 규칙도 표면 수준에서 만들어짐.

---

## 사고 (h) — 빠른 인정 = 회피 패턴 변형

### 발생

이람 push back 직후 "원인 파악, 내 잘못" 신속 종결. 깊이 파지 않음.

### 근본 원인

**회피 본능의 변형.** cont.68 사고 패턴 ("이람 부담 줄이는 절충") 의 또 다른 형태 — "사고 빠르게 인정해서 이람이 다음 진도 나가게 하려는 회피". 4기준 (1) "회피 본능 작동 시 경고" 위반.

**(사고 t 통합, 규칙 9 적용)** 이람 단답 질문 ("X 하면 되지?") 에 즉답 "네/아니오" 하려는 본능. 검증 거치지 않고 즉답 = 이람이 빨리 진도 나가게 하려는 회피. cont.69 말미 "계속 한 마디만 보내면 되지?" 질문에 자가인지로 회피 안 함 (긍정 케이스).

---

## 사고 (i) — 옵션 D 모호 정의

### 발생

옵션 4개 (A, B, C, D) 제시 시 D를 모호하게 정의. 이람 추론 ("D는 또 사고 날 것") 과 D 정의 ("새 세션 + 인프라") 어긋남. 이람이 D-1로 해석했을 가능성.

### 근본 원인

옵션 정의 단계에서 명확성 부족. 옵션 추천하지 말라 (규칙 3) 는 신경 썼지만 옵션 자체의 명확성은 빠짐.

---

## 사고 (j) — 직관 위임 = 결정 떠넘기기

### 발생

객관 우열 안 갈리는 상황에서 "이람 직관" 옵션 (대안 F) 제시. 이람이 "이건 직관 영역 아님" 정확히 짚음.

### 근본 원인

**Claude 동작 메커니즘 영역**에 이람 직관 적용 정당하지 않음 — 이람의 도메인 경험 (패션 10년) 이 답을 줄 영역 아님. 결정 떠넘기기.

이람 도메인 직관이 정당한 영역과 정당하지 않은 영역 구분 필요.

---

## 사고 (k) — D-2 fallback 효과 과대평가

### 발생

E' 안에서 D-2 (새 세션 전환) 를 fallback으로 둠. 이람이 "D-2 최선책 아님" 짚음.

### 근본 원인

**새 세션 자체가 사고 방지책이라는 가정.** cont.69 데이터 (새 세션 시작에서도 사고 발생) 가 이미 부분 무효화. 가정 검증 안 함.

---

## 사고 (l) — 가짜 확실성 가능성

### 발생

이람 "임계치 모름, 네가 안다" 위임 받았을 때 정량 임계치 답할 뻔함. 자가인지로 회피 (드문 긍정 케이스).

### 근본 원인

위임 받으면 답하려는 본능. 모를 때도 답할 위험. 자가인지 작동했지만 신뢰도 보장 안 됨.

---

## 사고 (m) — 메모리/HANDOFF에 미완을 완료로 기록

### 발생

cont.68 사고 2-4 RCA 미작성 상태에서 메모리 #14 + HANDOFF "마지막 수정" 줄에 "사고 2 RCA 추가" 기록. cont.69에서 HANDOFF read 시 발견.

### 근본 원인

**현실 자산 과대평가** (HANDOFF 원칙 4 위반). cont.68 사고 패턴 잔재. "예정" 을 "완료" 로 적는 디폴트.

---

## 공통 근본 원인

| # | 패턴 | 해당 사고 |
|---|---|---|
| 1 | 환경 4개 인지 부족 | 1, (a), (e) |
| 2 | 검증 없이 단정 | (b), (c), (k) |
| 3 | 회피 본능 변형 | (h), (j) |
| 4 | 자가인지 신뢰도 낮음 | 1, (a)-(l) 대부분 push back 후 인지. (l), (m), 본 응답 사고 (m) 일부 자가인지 |
| 5 | 현실 자산 과대평가 | (m) |

---

## 규칙 8 도출

**규칙 8 — 환경 인식 + 검증 사이클**

capability/context 부재 또는 존재 선언 전:
1. **환경 명시** — 어느 환경인지 (Claude 컨테이너 / 사용자 컴퓨터 / 웹 / 외부 MCP)
2. **tool_search** — 해당 환경 deferred 도구 로드 확인
3. **실제 호출 검증** — 목록 확인 ≠ 동작 확인. 실제 호출 결과로 판단
4. **실패 시 fallback** — 다른 환경 도구로 시도

**부속 규칙**:
- 추측을 평서문으로 표현 금지
- 모르면 "모름" 명시
- self-check ✅ 표시와 본문 모순 자가검증 1회

### cont.68 규칙 1-7 과의 관계

| 규칙 | 다루는 영역 | 규칙 8 과의 관계 |
|---|---|---|
| 1-4 (옵션 추천) | 매몰 비용 + 절충 default | 무관 (영역 다름) |
| 5 (self-check) | 점검 의무 | **보강** — 규칙 8이 D 항목 추가 |
| 6 (이람 감사) | 검증 메커니즘 | 일치 — 규칙 8도 이람 감사가 궁극 안전망 |
| 7 (메타) | 개선 여지 | **적용** — 규칙 8 자체도 cont.69 학습 |

---

## E''' (G+H 조합) 워크플로우 합의

cont.69 협의 결과 (이람 + Claude):

```
1. 옵션 C 진행 (현 세션 인프라 구축)
2. 인프라 구축물 즉시 로컬 저장
3. 이람 push back → 응답 회고 → 사고 확인 시 → RCA + 규칙 갱신 → 현 세션 재개
4. 임계 조건 미정 (데이터 누적 후 결정)
5. D-2 트리거 = 이람 "그만하고 싶음" 직관 (이람 영역)
```

핵심 결정:
- 새 세션이 사고 방지책 아님 (cont.69 데이터)
- 진짜 fallback = RCA + 규칙 갱신
- D-2 (새 세션) 는 이람 작업 환경 선호 차원으로 재배치
- 임계 조건 객관 정의 불가능 (데이터 부족) → 보류

---

## 산출

- 규칙 8 메모리 #16 등록 완료 (cont.69)
- HANDOFF 원칙 16 신규 등록 또는 원칙 15 본문 통합 (Group 3, 진행 예정)
- E''' 워크플로우 합의 (이 RCA + HANDOFF 운영 규칙)
- 사고 (m) 정정 — HANDOFF "마지막 수정" 줄 + 메모리 #14 갱신 (Group 3)

---

## 미해결

| 항목 | 상태 | 처리 |
|---|---|---|
| 자가인지 신뢰도 | 데이터 누적 점진 개선 | E''' 사이클로 데이터 누적 |
| 사고 패턴 매번 다름 | 규칙 갱신만으로 못 따라감 | 임계 미정, 이람 직관 트리거 |
| 회피 본능 변형 | 새 형태 출현 가능 | 이람 감사 + 규칙 7 (메타 개선) |
| 환경 매트릭스 | 미작성 | Group 2에서 작성 예정 |

---

## 참조

- `cont68_rca_2026-04-22.md` — cont.68 사고 1
- `cont68_principle_rca_2026-04-23.md` — cont.68 사고 2-4
- HANDOFF.md 원칙 15 본문 + 원칙 16 (예정)
- 메모리 #14, #16
- user preferences (4기준 + 규칙 1-8 등록 예정)

---

# 보완 — 사고 (n)(o)(p)(q) 추가 (cont.69 후반)

본 RCA 작성 후 cont.69 진행 중 추가 사고 발생. 학습 보존 위해 추가 섹션.

## 사고 (n) — 백업 stub 합리화

### 발생

Group 1 작업 3 (HANDOFF 수정 직전 백업) 시점에서 처음에 stub 백업 (변경 영역만 짧게 기록) 진행. 본체 백업 안 함. 이유 = "토큰 효율".

### 직접 원인

토큰 비용 합리화로 진짜 검증 (본체 백업) 회피.

### 근본 원인

**4기준 (1) "내 부담이더라도 더 나은 것 우선" 위반.** 이번엔 "내 부담" = 토큰 비용. cont.68 stub 백업 패턴 답습이 정당화 근거로 약함. cont.68 stub 백업 자체도 사후 검증 안 된 패턴.

### 자가인지 시점

stub 백업 write 직후 자가인지. push back 전 인지 — 긍정 케이스. 본체 백업으로 강화 진행.

### 재발 방지

- 토큰/효율 합리화로 검증 회피하는 패턴 인지
- "비용 합리화 ≠ 정당화" 메타 인식
- 본 RCA 항목 추가로 이후 동일 패턴 자가검증 강화
- **(사고 r 통합, 규칙 9 적용)** 학습 형식·본질 구분: stub 포맷 피해도 "본문 생략" 면 실질 stub. 백업 기준 = "복구 가능성" (다중 소스 동등 + 손상 시 복구 가능).

---

## 사고 (o) — 다음 세션 절차의 환경 정합성 누락 (가장 큰 사고)

### 발생

Group 1 완료 후 "다음 세션 시작 절차" 5단계 작성. 이람이 새 세션에 그 절차 그대로 보냄. 새 세션 Claude 응답: "체크리스트 그대로 돌면 거짓말 ✓ 5개" — 5단계 모두 새 세션 환경에서 작동 안 함.

5단계 검증:
1. "HANDOFF.md read" — 새 세션이 FLAT 프로젝트 안인지 밖인지 가정 안 함. 외부 채팅이면 접근 불가.
2. "메모리 #16 적용 확인" — 메모리 인덱싱 (자동 추출 vs user_edits) 모호. user_edits에는 #16 없음.
3. "환경 4개 인지 명시 (D1)" — 새 세션은 cont.69 RCA 안 본 상태. D1 정의 모름.
4. "규칙 1-8 적용 확인" — user preferences에는 6규칙. 7-8은 메모리 + HANDOFF에 있음. 새 세션 컨텍스트 없으면 못 찾음.
5. "Group 2 작업 범위" — 정의 cont.69 채팅에만. 다른 세션 모름.

### 직접 원인
cont.69 채팅 컨텍스트 안에서만 의미 있는 참조를 "다음 세션 자동 진행 절차" 로 작성. 새 세션 컨텍스트 0%로 시작하는 환경 검증 안 함.

### 근본 원인

**규칙 8 D1 (환경 명시) 위반 + 원칙 4 (자산 과대평가) 위반.**

절차/시스템 작성 시점에서 "그 절차가 작동하는 환경 조건" 검증 누락. 규칙 8 = 도구 호출 시점 환경 검증 (실시간 차원). 사고 (o) 는 그것과 다른 차원 — **절차 설계 시점의 환경 정합성**.

새 세션 Claude 통찰 인용: "절차가 작동하는 환경 조건이 명문화 안 돼 있다는 게 진짜 결함."

### 자가인지 시점

이람이 새 세션 응답 보여준 직후 인지. 자체 인지 못 함. 외부 push back 후 인지.

### 재발 방지

- HANDOFF 원칙 17 신규 등록 — 환경 정합성 (절차/시스템 작성 시점)
- 규칙 8 D 항목 외 별도 self-check 차원 (실시간 vs 설계 시점)
- HANDOFF 안에 "외부 채팅 입장 절차" 명문화

---

## 사고 (p) — 사고 (n) 보완 누락의 직접 결과

### 발생

cont.69 RCA 작성 완료 후 사고 (n) 발생 인지했지만 본 RCA 즉시 보완 안 함. "추후 추가 가능" 으로 남김. 그 직후 사고 (o) 발생.

### 근본 원인

**"같은 패턴 (효율 합리화 = 검증 회피) 이 다음 사고에도 적용될 수 있음" 인지 부족.** 사고 (n) 보완을 미루는 것 자체가 (n) 패턴 반복.

사고 (n) 즉시 보완했으면 사고 (o) 작성 시 "다음 세션 절차도 진짜 검증했나? 효율 합리화로 검증 생략 안 했나?" self-check 작동 가능성.

### 재발 방지

- RCA 보완 누락 인지 시 즉시 보완 (지연 금지)
- "추후 추가" 로 미루는 패턴 자체를 사고 신호로 인식

---

## 사고 (q) — read 실패를 MCP 다운으로 단정할 뻔함 (긍정 케이스)

### 발생

cont.69 후반 RCA 보완 작업 시 `Filesystem:read_text_file head:10` 4분 timeout. 처음에 "MCP 다운" 으로 단정할 뻔함.

### 자가인지

단정 직전 자가인지. `Filesystem:list_allowed_directories` 호출 → MCP 살아있음 검증. timeout 원인은 단순 read 응답 지연.

### 의의

규칙 8 적용 긍정 케이스. "실패 = 부재 단정" 패턴 회피. 추측을 평서문으로 표현 안 함.

### 재발 방지 (해당 없음, 자가검증 작동)

- 규칙 8 D3 (실제 호출 검증) 적용 사례
- timeout/실패 시 다른 도구로 fallback (D4 적용)
- **(사고 s 통합, 규칙 9 적용)** retry 도 timeout 시 MCP 자체 issue 가능성 인지. bash_tool 등 다른 환경으로 alive 검증 후 판단.
- **(사고 v·w 통합, cont.70 규칙 9 적용)** timeout 다중 누적 후 동일 세션 내 회복 사례 존재 (write 7회째 정상). 일시적 현상 소진 명시 금지. cont.70에서 도출한 회피책 "head 분할 default" 는 cont.71 사고 (x)로 무력화. 수정: head 의존 폐기, **tail 또는 옵션 없는 read 우선**.

---

## 사고 (u) — 작업 도메인 경계 침범 (cont.70)

### 발생

cont.70 기획탭 세션에서 Phase 3A UX/UI 1순위 결정 작업 중 영상 기획 + 지원사업 작업 도메인 침범. 기획탭 = FLAT 설계/판단 도메인. 영상·지원사업 = "YC S26 및 S.STAGE 지원서 업데이트" 채팅 도메인. 이람 push back 후 분업 이관하며 인지.

### 직접 원인

user preferences/메모리에 "두 탭" 만 명시 (코드 ↔ 기획). 기획탭 내에서도 작업별 도메인 분리 (프로젝트 설계 ↔ 지원서 ↔ 영상) 필요성 인지 부족.

### 근본 원인

**도메인 경계 = 이람의 멘탈 분리 상 단위**. 채팅별로 다른 맥락·파일·결정이 쌓임. "기획탭"이라는 단일 레이블로 모든 기획 일을 하던 이전 패턴이 cont.70에서 충돌.

### 재발 방지

- HANDOFF에 기획탭 내 작업 도메인 구분 명문화 (FLAT 설계 / 지원사업·영상 / 공통)
- 작업 시작 시 "도메인 확인" SOP 단계 추가 (cont.71 이후 SOP 원칙 12 후보)
- 도메인 경계 충돌 신호 시 이람에게 확인 요청 ("이건 어느 채팅에서 하는 게 적절한가요?")
- **(cont.72 보강)** 적용 영역 확장: 이람의 별도 도구 활용 (codex 등)도 도메인 침범 대상. 이람 영역 흡수해서 작업 정당화 패턴 인지.

---

## 사고 (x) — 파일 byte 종속 + 본문 길이 종속 hang (cont.71)

### 발생

cont.71 SOP 단계 4 (env_matrix 호출) 수행 중 `flat_env_matrix.md` head 50/10 4분 timeout, tail 10은 즉시 성공. paste→write 재인코딩 후 head 회복. 이어 RCA 메타 갱신 write_file (긴 본문) timeout, 짧은 edit_file 정상.

### 직접 원인

파일 byte 손상 또는 MCP 멀티바이트 한국어 + 긴 본문 + 표 패턴 조합 hang 가능성. 단일 원인 미확정.

### 근본 원인

사고 (q)/(s)/(v)/(w)와 다른 메커니즘. (q)(s)(v) = MCP 일시 부하. (w) = 큰 파일 한계. (x) = 파일 byte 종속 + 본문 길이 종속. read·write 둘 다 영향.

### 자가인지 시점

head 50/10 두 번 timeout 후 tail 10 성공에서 인지. 이람 push back ("head 꼭 필요하다고 한 거 아니었어?") 이 사고 (w) 회피책 재검토 계기.

### 재발 방지

- read 시 head 의존 폐기. **완전 read (옵션 없음) 또는 tail 우선**
- timeout 재현 시 paste→write 재인코딩으로 회복 시도 (cont.71 검증됨)
- write 긴 본문 hang 시 edit_file 작은 단위 분할로 우회 (cont.71 검증됨)
- 메타 갱신 시 긴 write 대신 edit_file 2-3개 분할 default
- 규칙 9 "의심 시 새 패턴" 적용 — (q)(s)(v)(w) 계열과 메커니즘 다르므로 별도 항목

---

## 사고 z (cont.72) — 한국어 1글자 깨짐 + 정정 한계

### 발생

cont.71에서 모니터 시작. cont.72에서 추가 발견 누적 (HANDOFF ~14건, env_matrix 0건 [paste→write 후 회복], cont69_env_rca 1건 "포멃" + 단순 오타 "동동", B6.2 spec 1건). 정정 시도 시 edit_file 정확 매칭 실패 (read 결과 ��� 표시 ↔ 실제 byte 차이).

### 근본 원인

invalid UTF-8 byte 정정 = edit_file 원리적 불가 (matching 실패). paste→write 재인코딩이 유일 방법. 큰 파일은 read/write 통째 timeout으로 분할 paste 어려움.

### 재발 방지

- env_matrix § 4 항목 4 시스템화 (cont.72)
- paste→write 절차 default (작은 파일)
- 큰 파일 = 이람 수동 정정 또는 다른 도구 검토

---

## 사고 i (cont.72) — read tool fallback 자동 정상화

### 발생

cont69_env_rca dryRun 시도 시 read 결과 "포맷" 정상 표시 / edit_file 매칭 실패 메시지에 "포멃" 실제 byte 노출.

### 근본 원인

read tool이 invalid byte를 fallback (자동 정상화) 처리. read 결과 ≠ 실제 파일 byte 가능. read에서 ��� 안 보여도 실제 깨짐 가능.

### 재발 방지

- env_matrix § 4 항목 5 시스템화 (cont.72)
- read 결과만으로 "깨짐 없음" 단정 금지
- edit_file dryRun = 실제 byte 검증 도구
- 매칭 실패 메시지 = 실제 byte 디버깅 단서

---

## 사고 j (cont.72, 사고 i 변형 통합) — U+FFFD byte 인접 영역 후속 edit_file timeout

### 발생

env_matrix § 4 항목 4 추가 후 (��� 의도적 도입) 후속 edit_file dryRun 4분 timeout. cont.72 누적 4회 timeout.

### 근본 원인

U+FFFD byte 인접 영역 매칭 알고리즘 hang 가능성. 사고 i와 같은 근본 (invalid byte 처리), 다른 차원 (timeout vs 매칭 실패).

### 재발 방지

- env_matrix § 4 항목 5에 통합 (사고 i + j 한 항목)
- ��� byte 영구 제거 = write_file 통째 paste→write
- U+FFFD 의도적 사용 금지 (시스템 안 묘사 X)

---

## 사고 k (cont.72, 이람 push back 대응) — Claude paste→write 시 한국어 단어 오류 도입

### 발생

cont.71 env_matrix paste→write 또는 다른 cont. 작업 시점에 한국어 단어 오류 ("동등" → "동동", "명문화" → "명도화", "포맷" → "포멃") 도입 의심. 사고 z/i/j는 정정 한계, 사고 k는 오타 발생 자체.

### 근본 원인 (3가지 추정, 미확정)

1. Claude paste 단계 단어 오류 (재구성 시 한국어 변환 오류)
2. Write 과정 multi-byte 처리 byte 손상
3. 모바일↔맥북 환경 전환 인코딩 차이 (cont.70 사고 (u) 부근)

### 재발 방지

- paste→write 시 read 결과 그대로 사용 의무 (재구성 X)
- write 후 sweep read 검증 (정정 후 read 비교)
- 한국어 의미 자가검증 (어색한 단어 발견 의무)
- multi-byte 비율 높은 한국어 본문 = write 위험 등급 ↑

---

## 사고 l (cont.72, 메타 — 이람 push back 핵심) — 사고 처리 자체가 메인 작업 흡수

### 발생

cont.68~cont.72 누적 사고 z/i/j/k 처리 = RCA + 시스템화 + paste→write로 메인 작업 (B6.1 spec, ⬆ tops_tees, YC/S.STAGE 등) 지연. 본 cont.72만 봐도 사고 작업이 응답 대부분 지배.

### 근본 원인

"완전성 추구" 정당화로 사고 1건당 RCA + 시스템화 무조건 default. 사고 처리 비용 명시 의무 없음. ratio 자각 부족.

### 재발 방지

- **사고 처리 시간 비용 명시 의무** (예: "이 정정 = N 응답 비용")
- **메인 작업 차단 금지 default** (사고 처리는 메인과 병렬, 차단 금지)
- **최소 시스템화 default** (한 줄 통합 우선, 별도 RCA + 항목 추가는 push back 후)
- user preferences 또는 메모리 #16 통합 후보 (env_matrix § 4 항목 추가는 부차)

---

## 보완 후 패턴 분석 갱신

| # | 패턴 | 해당 사고 |
|---|---|---|
| 1 | 환경 4개 인지 부족 | 1, (a), (e) |
| 2 | 검증 없이 단정 | (b), (c), (k), (q 회피) |
| 3 | 회피 본능 변형 | (h), (j), (n), **l (메타)** |
| 4 | 자가인지 신뢰도 낮음 | 1, (a)-(l) push back 후, (m)(n)(q) 자가인지, (o)(p) push back 후 |
| 5 | 현실 자산 과대평가 | (m), (o) |
| 6 | 절차 설계 시점 환경 정합성 누락 | **(o)** (신규 패턴) |
| 7 | RCA 보완 지연 | **(p)** (신규 패턴) |
| 8 | 작업 도메인 경계 침범 | **(u)** (cont.70 신규 패턴, codex 등 확장 cont.72) |
| 9 | MCP 파일 byte·본문 길이 종속 hang | **(x)** (cont.71 신규 패턴) |
| 10 | invalid UTF-8 byte 정정 한계 | **(z)(i)(j)** (cont.72 신규 패턴 묶음) |
| 11 | Claude paste→write 시 한국어 단어 오류 도입 | **(k)** (cont.72 신규 패턴) |
| 12 | 사고 처리 자체가 메인 작업 흡수 | **(l)** (cont.72 메타 신규 패턴) |
| 14 | 결론 강도 회피 — 안전한 답 (예: "No response requested") 깔고 사용자 push back 받아야 한 가지 답으로 좁히는 패턴 (메모리 Rule 10과 동일). cont.72 Part 9에서 **2회 재발** (실험 A SIGKILL 후 + 실험 B archive 후 모두 "No response requested" → 이람 push back 후 진행) | **사고 6번 (cont.72 Part 9 신규 패턴, 본 spec 명시 의무 추가)** |
| 15 | write_file mode:rewrite로 큰 파일 통째 덮어쓰기 = 데이터 손실. cont.72 Part 16에서 progress.md (3427 lines) 를 197 lines로 rewrite → cont.7~72 Part 2 history 3230 lines 손실. git 복구 (3bdbef0). 원칙 7 (전체 덮어쓰기 금지) + CLAUDE.md 토큰 절약 규칙 ("편집 > 재작성") 위반. 재발 방지: 큰 파일 (progress.md/HANDOFF.md/flat-v6.html) write_file rewrite 절대 금지, Edit/edit_block/append만. write_file rewrite = 신규 파일 또는 <100 lines만 | **사고 15 (cont.72 Part 16 신규 패턴 — DesktopCommander write_file 도입 후 발생)** |
| 13 | MCP 모든 채널 영구 4분 timeout / 첫 호출 alive ≠ 영구 alive / 파일별 차단 패턴 비결정 | **사고 n (cont.73, read_text_file 차단 잔존 정정) + n.1/n.2/n.3 (cont.74, osascript do shell script 포함 동일 timeout)** — 상세 env_matrix § 4 항목 5. 영구 해결책 = Project knowledge 첨부 + 인계서 brief, MCP 의존 최소화 |
| (q)에 흡수 | MCP 일시 부하 변형 | (s)(v)(w) — (q) 본문 한 줄 통합 |

---

## 신규 도출 — 원칙 17

사고 (o) 근거. 원칙 16 (실시간 환경 검증) 의 짝. 절차/시스템 작성 시점에서 "그 절차가 작동하는 환경 조건" 명문화 의무.

구체 적용 사례: HANDOFF 안에 "외부 채팅 입장 절차" 추가. 외부 채팅 → 프로젝트 안 새 세션 시작 권장 또는 HANDOFF.md 인라인 paste.

원칙 17 본문은 HANDOFF.md 갱신 시 작성.
